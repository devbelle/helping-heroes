<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Helping Heroes</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
    <link rel="stylesheet" href="/css/styles.css" type="text/css" media="screen" />
  </head>
  <body>
  </head>
  <body>
    <header>
      <nav>
        <a href="/"><button class="button is-link m-1">Home</button></a>
        {{#if logged_in}}
        <a href=""><button class="button is-link m-1">Posts</button></a>
        <a href=""><button class="button is-link m-1">My Profile</button></a>
        <button class="button logout is-link m-1">Logout</button>
        {{#if is_admin}}
        <a href="/admin"><button class="button is-link m-1">Admin</button></a>
        {{/if}}
        {{else}}
        <a href="/login"><button class="button is-link m-1">Login</button></a>
        {{/if}}
      </nav>
      <h1 class="title is-size-2">Welcome to Helping Heroes!</h1>
    </header>

    <main class="container">
      {{{body}}}
    </main>
    <script src="/js/logout.js"></script>
    <script type="module" src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"></script>
    <script nomodule src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.js"></script>
  </body>

</html>
